# 합력벡터_제어.ino 변경 이력

[`합력벡터_제어.ino`](합력벡터_제어.ino) — 2단계. 목표각에 합력 벡터를 넣은 코드.

1단계는 [`../[1단계] 수평제어/모터_수평제어_I2C.ino_변경이력.md`](../%5B1%EB%8B%A8%EA%B3%84%5D%20%EC%88%98%ED%8F%89%EC%A0%9C%EC%96%B4/%EB%AA%A8%ED%84%B0_%EC%88%98%ED%8F%89%EC%A0%9C%EC%96%B4_I2C.ino_%EB%B3%80%EA%B2%BD%EC%9D%B4%EB%A0%A5.md) 참고.

| 푸시 시각 | 커밋 | 내용 |
|---|---|---|
| 2026-08-29 22:03 | `aafca03` | [최초 작성 — 합력 벡터 목표각 추가](#무엇이-바뀌었나) |
| 2026-08-29 22:47 | `c43658f` | [roll 축 중력 제거 부호 수정](#1차--중력-제거-부호) |
| 2026-08-29 23:06 | `d98757d` | [목표각 부호 수정 + 두 축 대칭화](#2차--목표각-부호) |
| 2026-08-29 23:14 | `2e456da` | [부호 실측 확인 — `DIR_ACC` 확정](#부호-실측-확인-2026-08-29-2310) |
| 2026-08-29 23:24 | `2c7a5a1` | [안쪽축 회전 부호 반전 + `dp`/`dr` 명령](#안쪽축이-반대로-돌고-있었다) |
| 2026-08-29 23:38 | `HASH` | [부호 반전 되돌림 + `DIR` 을 전송 시점으로 이동](#손에-든-imu-로는-부호를-정할-수-없다) |

---

## 왜 수평이 정답이 아닌가

가속하는 차 안의 물컵을 생각하면 된다. 컵을 수평으로 붙잡고 있으면 물은
뒤로 쏠린다. 액체 입장에서는 중력과 관성력의 **합력**이 진짜 아래쪽이므로,
컵 바닥을 그 합력에 수직으로 놓아야 수면이 컵 기준으로 평평해진다.

| 상황 | 합력 | θ_ref |
|---|---|---|
| 정지·등속 | 중력만 | **0** — 수평이 정답 |
| 가속 중 | 중력 + 관성력 | **≠0** — 기울여야 정답 |
| 경사면 + 가속 | 둘의 합 | 자동으로 합성 |

**수평제어와 안티슬로싱이 별개의 모드가 아니다.** 한 식이 두 경우를 모두 덮는다.
경사면에 서 있으면 `a_linear` 가 0 이라 `θ_ref` 도 0 이 되어 자동으로 절대 수평이 된다.

> 초기에 "1. 수평제어 / 2. 안티슬로싱" 으로 모드를 나누려 했으나 그렇게 하면
> 경계에서 채터링이 생기고, 그 덜컥거림 자체가 슬로싱을 새로 만든다.
> 험지 주행처럼 경사와 가속이 동시에 오는 상황도 어느 모드로도 처리가 안 된다.

---

## 무엇이 바뀌었나

1단계에서 **목표각 한 항만** 추가됐다.

```
1단계 :  θ_ref = 0                    →  motor_cmd = −θ_base
2단계 :  θ_ref = atan(a_linear / g)   →  motor_cmd = θ_ref − θ_base
```

### 계산 순서

```cpp
// 1. 중력 성분 제거 — 가속도계는 기울어져 있기만 해도 중력을 읽는다
float one_g   = G_LSB * g_scale;
float a_lin_y = ay - one_g * sinf(pitch_filtered * DEG_TO_RAD);
float a_lin_x = ax - one_g * sinf(roll_filtered  * DEG_TO_RAD);

// 2. 합력이 수직에서 벗어난 각도
float raw_ref_p = atan2f(a_lin_y, one_g) * RAD_TO_DEG * DIR_ACC;

// 3. 노이즈가 크므로 목표각을 따로 저역통과
ref_pitch = ACC_LPF * raw_ref_p + (1 - ACC_LPF) * ref_pitch;

// 4. 모터는 차체 기준으로 도므로 차체 자세를 뺀다
want_pitch = (ACC_GAIN * ref_pitch - pitch_filtered * GAIN) * DIR_PITCH;
```

중력을 뺄 때 쓰는 각도가 1단계에서 만든 `θ_base` 다. `g_scale` 은 부팅 시
정규화한 값이라 1g 에 해당하는 LSB 가 `G_LSB × g_scale` 이다.

### 새 파라미터

| 이름 | 기본 | 명령 | 뜻 |
|---|---|---|---|
| `ACC_GAIN` | **0** | `ag<값>` | 합력 항 게인. **0 이면 1단계와 완전히 동일** |
| `DIR_ACC` | +1 | (코드) | 합력 목표각 부호. 실측으로 정함 |
| `ACC_LPF` | 0.2 | `ad<값>` | θ_ref 전용 저역통과 |
| `ACC_REF_LIMIT` | 20° | (코드) | θ_ref 자체의 상한 |

### 출력에 `ref_*` 추가

```
pitch:12.34,ref_pitch:-4.20,cmd_pitch:-12.10,act_pitch:-11.8,roll:...
                  ↑ 합력 목표각
```

`ACC_GAIN` 이 0 이어도 계산되어 찍힌다. **모터를 켜지 않고 부호를 확인**할 수 있다.

---

## 기본값을 0 으로 둔 이유

부호가 반대면 가속할 때 액체를 **더** 흔드는 쪽으로 기울인다.

수평제어는 부호가 틀리면 트레이가 반대로 기우는 것이 바로 보인다. 그런데
합력 항은 **가속하는 순간에만** 나타나므로 눈으로 알아채기 어렵다.
그래서 처음 올리면 1단계와 똑같이 동작하게 두고, 숫자로 확인한 뒤 켠다.

되돌릴 때도 `stop` 보다 **`ag0`** 이 먼저다. 수평제어는 계속 살아 있다.

---

## 부호 확인 절차

### 1) 모터 없이 부호만

`arm` 하지 않고 IMU 를 책상에서 한 방향으로 민다. `ref_pitch` 를 본다.

| 보드 **+Y 방향**으로 밀었을 때 | 판정 |
|---|---|
| `ref_pitch` 음수 | ✅ 맞다 (`DIR_ACC = +1` 유지) |
| `ref_pitch` 양수 | ❌ `DIR_ACC` 를 `−1` 로 |

음수여야 하는 이유. 컵을 +Y 로 가속하면 물은 −Y 로 쏠린다. 수면을 컵 기준으로
평평하게 두려면 컵의 +Y 쪽을 내려야 한다. 우리 규약에서 `+pitch` 는 +Y 가
올라가는 것이므로 목표는 음수가 된다.

roll 은 `+roll` 이 +X 가 **내려가는** 것이라 부호 방향이 pitch 와 반대다.

### 2) 정지 상태 확인

- 책상에 가만히 → `ref` 가 0 근처
- **기울여 놓고 가만히 → 이때도 0 근처**

기울기만으로 `ref` 가 생기면 중력 제거가 잘못된 것이다. `g_scale` 과 `θ_base`
를 먼저 의심한다. **이 경우 `ACC_GAIN` 을 올리면 안 된다.**

### 3) 게인 상승

```
ag0.3  →  ag0.6  →  ag1.0
```

각 단계에서 IMU 를 밀었다 세웠다 하며 트레이가 가속 반대쪽으로 기우는지 본다.

### 기록할 것

| 항목 | 값 |
|---|---|
| `DIR_ACC` 최종 부호 | **`+1`** — 실측 확인 (2026-08-29 23:10) |
| 정지 시 `ref` 편차 | 0 근처 — 평평·경사 모두 확인 |
| 안정적인 최대 `ACC_GAIN` | (미측정) |
| `ACC_LPF` 적정값 | (미측정) |

---

## 아직 없는 것

**ZV 입력성형이 없다.** 지금은 `θ_ref` 가 바뀌는 즉시 그대로 따라가므로,
목표각의 급변 자체가 슬로싱을 새로 만들 수 있다. 3단계에서 `θ_ref` 에
Convolved ZV 를 걸어 그 성분을 없앤다.

**지연이 150~180ms 로 측정돼 있다.** 슬로싱 주기 420ms 의 36% 라 위상 여유가
부족하다. `CMD_LPF`(57ms)·슬루·낮은 `Kp` 가 원인이며, 3단계 전에 손봐야 할 수 있다.

---

## 다음 단계

- [ ] 부호 확인 (`DIR_ACC`)
- [ ] 정지·경사 정지 상태에서 `ref` ≈ 0 확인
- [ ] `ACC_GAIN` 을 1.0 까지 상승
- [ ] 지연 단축 검토 — 안티슬로싱에 직접 영향
- [ ] 3단계: `θ_ref` 에 Convolved ZV 적용 (roll 축 포함 — 이전 구조의 누락 사항)


---

## 부호를 두 번 틀린 기록

두 축의 각도 정의가 반대라는 사실이 두 곳에서 각각 터졌다. 같은 원인인데
증상이 달라서 한 번에 못 잡았다.

```
pitch 는  atan2( ay, ...)  로,   roll 은  atan2(-ax, ...)  로 정의했다
```

### 1차 — 중력 제거 부호

`c43658f` · 2026-08-29 22:47

중력 성분을 두 축 다 빼도록 썼다.

```cpp
a_lin_y = ay - one_g * sin(pitch);   // 맞음
a_lin_x = ax - one_g * sin(roll);    // 틀림
```

`roll` 규약에서는 `ax = −g·sin(roll)` 이라 중력 몫이 이미 음수다. 여기서 또
빼면 상쇄가 아니라 **두 배**가 된다.

증상이 명확했다. 평평하게 두었는데 실제 `roll` 이 2.5° 였을 때:

```
ax       = −g·sin(2.5°) = −0.0436 g
a_lin_x  = −0.0436g − 0.0436g = −0.0872 g      ← 두 배
ref_roll = atan(−0.0872) = −5.0°               ← 실측과 일치
```

조금만 기울여도 `ACC_REF_LIMIT` 20° 에 포화됐다. `ref_pitch` 는 멀쩡했고,
그 비대칭이 식이 아니라 축을 가리켰다.

**정지 상태 확인 절차가 잡아낸 것이다.** 가속 시험부터 했으면 `ref_roll` 이
이미 포화돼 있어 부호 판정 자체가 불가능했다.

### 2차 — 목표각 부호

`d98757d` · 2026-08-29 23:06

`DIR_ACC` 하나를 두 축에 공유했다. 그런데 축마다 필요한 부호가 다르다.

가속도계는 비력 `f = a − g` 를 읽는다. 평평한 상태에서 `+Y` 로 `A` 만큼
가속하면 `ay = +A` 다.

| 축 | 코드 결과 | 있어야 할 값 | 판정 |
|---|---|---|---|
| pitch | `+atan(A/g)` | `−atan(A/g)` | ❌ 반대 |
| roll | `+atan(A/g)` | `+atan(A/g)` | ✅ 맞음 |

`−atan(A/g)` 인 이유. 유효중력이 `(0, −A, −g)` 라 트레이 법선은 `+Y` 쪽으로
기울어야 하고, 그것은 `+Y` 모서리가 **내려간다**는 뜻이다. 우리 규약에서
`+pitch` 는 `+Y` 가 올라가는 것이므로 목표각은 음수다.

**정지 상태로는 절대 안 보인다.** 가속할 때만 나타나고, 밀기와 멈추기가 항상
쌍으로 나와 로그의 부호만 봐서는 구분되지 않는다. 유도로 잡았다.

### 고친 방식 — 축마다 부호를 두지 않는다

`roll` 의 가속도 성분을 처음부터 `−ax` 로 잡아 두 축을 같은 식으로 만들었다.

```cpp
float acc_p =  (float)ay;     // pitch 규약
float acc_r = -(float)ax;     // roll  규약  ★ 각도 정의와 동일

float p_lin = acc_p - one_g * sinf(pitch_filtered * DEG_TO_RAD);
float r_lin = acc_r - one_g * sinf(roll_filtered  * DEG_TO_RAD);

float raw_ref_p = -atan2f(p_lin, one_g) * RAD_TO_DEG * DIR_ACC;
float raw_ref_r = -atan2f(r_lin, one_g) * RAD_TO_DEG * DIR_ACC;
```

중력 제거도 목표각도 두 축이 대칭이라 규약을 틀릴 자리가 한 곳뿐이다.
앞의 `−` 는 물리(가속 반대쪽으로 기운다)이고, `DIR_ACC` 는 실측용 뒤집기로
`+1` 에 남겼다.

---

## 가속 실측 (2026-08-29 23:00)

`capture.py --cmd none` 으로 29초, 954샘플. 모터는 끈 상태.

| 항목 | pitch | roll |
|---|---|---|
| `ref` 범위 | −17.8 ~ +18.7° | −19.0 ~ +19.2° |
| 포화 | 0회 | 0회 |

1차 수정이 맞았음을 확인했다. `ref_roll` 이 더 이상 포화되지 않고 두 축이
대칭으로 움직인다.

신호는 노이즈가 아니다. 5.0초 부근 파형:

```
5.00  +14.41
5.05  +17.14     ← 양의 피크 (밀기 시작, 가속)
5.09   +4.22
5.12   -7.60
5.17  -16.70     ← 음의 피크 (멈추기, 감속)
5.23   -3.95
```

**양 피크 → 음 피크 쌍**이 0.2초 간격으로 반복된다. 밀고 멈추는 동작의
교과서적 신호다. 크기 17° 는 0.3g 에 해당한다.

이 쌍 구조 때문에 로그만으로는 부호를 못 가린다. 한 번 밀 때마다 양수와
음수가 모두 나오므로, 어느 쪽이 "미는 순간"인지 알려면 민 방향을 알아야 한다.
2차 부호 문제를 유도로 잡은 이유가 이것이다.

---

## 부호 실측 확인 (2026-08-29 23:10)

밀기와 멈추기가 쌍으로 나오면 부호를 못 가린다. 그래서 **한 방향으로만** 밀고
되돌릴 때는 들어 올렸다. 복귀 구간에 책상면 방향 가속이 안 실리므로 각
이벤트의 첫 피크가 곧 미는 방향이 된다. 33초, 1185샘플, 모터는 끈 상태.

`|peak| ≥ 12°` 인 강한 이벤트만 추린 결과.

```
ref_pitch   -17 -17 -17 [+15] -15 [+15] -16 -18 -17 -18 [+14] -17 -17 -18 -16 -16 [+14] -16
ref_roll    +14 +13 +13  +12  +13  -12  +15 +15 +13 +13  +16  +14 +15 +13
```

`ref_pitch` 의 양수 4개(대괄호)는 모두 바로 앞 음수의 **0.2초 뒤**다. 미는 게
아니라 멈추는 감속이다. 1.3~1.7초 간격으로 규칙적인 진짜 밀기는 전부 음수다.

| 축 | 밀 때 부호 | 유도한 예상 | 판정 |
|---|---|---|---|
| `ref_pitch` | 음수 (14/14) | 음수 | ✅ |
| `ref_roll` | 양수 (13/14) | 양수 | ✅ |

**`DIR_ACC = +1` 확정.**

### 두 축이 한 번에 검증된 이유

`ref_roll` 이 +13~16 으로 크게 나왔다. 순수하게 `+Y` 로만 밀었다면 0 이어야
한다. `ax/ay ≈ 0.75` 이므로 `+Y` 에서 `+X` 쪽으로 40° 쯤 비스듬히 민 것이고,
덕분에 두 축을 동시에 확인했다.

두 채널은 각각 `ay` 와 `ax` 를 따로 쓴다. 서로 새어 들어갈 경로가 없으므로
`ref_roll` 응답은 실제 X축 가속이다.

---

## 슬루 제한이 병목이다

`ref` 가 **0.2초 만에 17°** 움직인다. 85°/s 인데 `MAX_RATE` 는 **30°/s** 다.
그 0.2초 동안 트레이는 6° 밖에 못 따라간다.

손으로 툭 미는 0.3g 는 실제 주행보다 훨씬 거친 입력이라 이 숫자를 그대로
걱정할 것은 아니다. 다만 1단계에서 측정한 **지연 150~180ms 와 같은 뿌리**다.
슬로싱 주기 420ms 의 36% 라 위상 여유가 부족하다.

3단계 전에 손볼 후보.

| 항목 | 현재 | 영향 |
|---|---|---|
| `MAX_RATE` | 30°/s | 큰 `ref` 변화를 잘라낸다 |
| `CMD_LPF` | 0.15 (57ms) | 위상 지연 |
| `MOTOR_KP` | 1.0 | `act` 가 `cmd` 의 78% 까지만 (드룹) |

---

## 안쪽축이 반대로 돌고 있었다

`2c7a5a1` · 2026-08-29 23:24

IMU 를 기울여 보니 두 축의 반응이 갈렸다.

| 물리 축 | CAN | 코드 채널 | 관찰 | 조치 |
|---|---|---|---|---|
| 바깥축 | `0x01` | `CAN_ID_ROLL` | 기울기 **반대로** 돈다 | `+1` 유지 |
| 안쪽축 | `0x02` | `CAN_ID_PITCH` | 기울기와 **같은 쪽**으로 돈다 | **`−1` 로 반전** |

같은 쪽으로 도는 것은 기울기를 키우는 **양의 되먹임**이다. 게인을 올리면
발산한다. `ACC_GAIN` 을 올리기 전에 잡아야 했다.

`DIR_PITCH` 는 제어식 전체에 곱해지므로 수평 항과 합력 항이 함께 뒤집힌다.
둘 다 트레이 기준 각도라 이것이 맞다.

```cpp
want_pitch = (ACC_GAIN * ref_pitch - pitch_filtered * GAIN) * DIR_PITCH;
```

### `dp` / `dr` 명령을 넣은 이유

이 부호는 **잠정값이다.** pitch/roll 과 모터의 배정 자체가 아직 임시고
(README 의 "미확정" 절), 지금은 IMU 를 손에 들고 있어 짐벌 축과 고정된 관계가
없다. 차체에 실장하면 다시 확인해야 한다.

| 명령 | 대상 |
|---|---|
| `dp` | `DIR_PITCH` 뒤집기 (안쪽축) |
| `dr` | `DIR_ROLL` 뒤집기 (바깥축) |

매번 컴파일하지 않고 확인하려는 것이다. 슬루 제한(`MAX_RATE` 30°/s)이 걸려
있어 부호를 바꿔도 계단이 아니라 램프로 넘어간다. 다만 기울어져 있을수록
이동량이 크므로 ARMED 상태에서 치면 경고를 띄운다.

> ⚠ `dp` / `dr` 은 `d<값>`(CAN 분주)보다 **먼저** 검사해야 한다.
> `ag` 를 `g` 보다 먼저 검사하는 것과 같은 이유다.

---

## 손에 든 IMU 로는 부호를 정할 수 없다

`HASH` · 2026-08-29 23:38

앞 항목에서 `DIR_PITCH` 를 `−1` 로 뒤집었다. **되돌렸다.** 관찰이 부호가
아니라 잡는 방향을 본 것이었다.

### 무엇이 관찰됐나

> IMU 를 어떻게 잡느냐에 따라 pitch 가 같은 방향으로 돌거나 roll 이 같은
> 방향으로 돌거나 한다. 그런데 **방향을 바꿔도 무조건 한 축은** 같은 방향으로
> 돈다.

이 "항상 정확히 한 축" 이 핵심이다. 단순한 부호 오류라면 부호를 맞추는 순간
두 축 모두 정상이 되어야 한다. 그렇게 안 되는 이유는 이렇다.

```
DIR_PITCH = -1     DIR_ROLL = +1      ← 서로 달랐다
```

IMU 가 짐벌에 붙어 있지 않으므로 `pitch` 가 어느 물리 축에 대응하는지는
**잡는 방향이 정한다.** 90° 돌려 잡으면 두 축의 담당이 맞바뀐다. 부호는
코드에 고정돼 있으니 어느 쪽으로 잡든 한 축이 어긋난다.

**따라서 차체에 실장하기 전에는 이 부호를 확정할 수 없다.** 지금 맞춰 놓아도
다음에 다르게 잡으면 또 틀린다. 기본값은 두 축 대칭(`+1`/`+1`)으로 되돌렸다.

### `DIR` 을 제어식에서 뺐다

부호를 뒤집을 때마다 화면 숫자까지 같이 뒤집히는 문제가 있었다.

```cpp
// 전
want_pitch = (ACC_GAIN * ref_pitch - pitch_filtered * GAIN) * DIR_PITCH;

// 후 — 제어식은 부호를 모른다
want_pitch = ACC_GAIN * ref_pitch - pitch_filtered * GAIN;
```

`DIR` 은 `sendBoth()` 에서만 곱한다.

```cpp
sendMIT(CAN_ID_PITCH, cmd_pitch * DIR_PITCH * DEG_TO_RAD, ...);
```

이제 `cmd_pitch` 는 `DIR` 과 무관하게 항상 물리적으로 맞는 값이다.
`pitch:+30` 이면 `cmd_pitch:−30` 이다. 제어식 `motor_cmd = θ_ref − θ_base`
그대로다.

`act_*` 출력에도 `DIR` 을 되곱해 `cmd_*` 와 같은 축척으로 맞췄다. 안 그러면
부호를 뒤집었을 때 지령과 실제가 반대로 보인다.

| 관심사 | 어디에 있나 |
|---|---|
| 제어 법칙 | `want_*` 계산 — 부호 없음 |
| 모터 배선·장착 방향 | `sendBoth()` — 여기서만 |

두 관심사를 한 노브에 묶으면, 부호를 바꿀 때마다 로그의 의미가 같이 바뀌어
무엇을 보고 있는지 알 수 없게 된다.
